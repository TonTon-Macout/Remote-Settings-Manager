<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет, когда поле в фокусе ("уплывшая" подсказка) -->
    <item android:color="?attr/textButtonBackgroundTint" android:state_focused="true"/>

    <!-- Цвет по умолчанию (до фокуса, подсказка внутри поля) -->
    <item android:color="#A80606"/>
</selector>